import { StyleSheet } from 'react-native';

export const styleHome = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
  },
  bgHome: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
  },
});
